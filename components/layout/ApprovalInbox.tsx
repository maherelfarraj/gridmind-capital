/**
 * Re-export shim — ApprovalInbox
 *
 * Canonical source: components/approvals/approval-inbox.tsx
 *
 * Import from either path:
 *   import { ApprovalInbox } from '@/components/layout/ApprovalInbox'
 *   import { ApprovalInbox } from '@/components/approvals/approval-inbox'
 */
export {
  ApprovalInbox,
  MOCK_APPROVAL_INBOX,
} from '@/components/approvals/approval-inbox'

export type {
  ApprovalRecord,
  ApprovalInboxProps,
  ApprovalStatus,
  SortOption,
  FilterOption,
} from '@/components/approvals/approval-inbox'
