import { ProjectRegistry } from '@/components/projects/project-registry'

export const metadata = { title: 'Project Registry — GridMind Capital' }

export default function Page() {
  return <ProjectRegistry />
}
