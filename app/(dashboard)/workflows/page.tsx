import { WorkflowEngine } from '@/components/workflow/workflow-engine'

export const metadata = { title: 'Workflow Engine — GridMind Capital' }

export default function Page() {
  return <WorkflowEngine />
}
